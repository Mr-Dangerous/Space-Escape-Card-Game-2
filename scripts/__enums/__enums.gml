enum card {
	inactive,
	in_hangar,
	moving
}

enum ship {
	planning,
	idle,
	scouting,
	moving,
	recon,
	returning,
	repositioning
}

enum camera {
	free_look,
	locked,
	all_encompassing
}

enum squad {
	moving,
	defend_sector
}